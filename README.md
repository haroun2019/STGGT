# A Spatial Temporal Graph Gated Transformer (STGGT)
STGGT is a hybrid deep learning model for traffic forecasting.
For execute the project you must download datasets, change paths, and import libraries.
